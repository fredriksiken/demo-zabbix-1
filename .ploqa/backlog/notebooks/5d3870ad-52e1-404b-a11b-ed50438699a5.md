# Workflow notebook – Add traffic-light status dashboard widget
_Backlog ID: 5d3870ad-52e1-404b-a11b-ed50438699a5_

## Enrich — 2026-04-28 19:16 UTC
- **Status:** SUCCEEDED
- **Board:** EXECUTION
- **Workflow run:** N/A

### Summary
```json
{
  "title": "Add traffic-light status dashboard widget",
  "description": "## Goal\nAdd a new dashboard widget that evaluates a configured numeric value or monitored numeric metric against thresholds and displays a traffic-light status indicator with explicit green/yellow/red, no-data, and invalid-configuration states.\n\n## Scope\nThis ticket covers a single dashboard widget type end-to-end: widget registration, persisted configuration, server-side validation, value resolution, status evaluation, refresh/update behavior, and frontend rendering. The widget must fit into the existing dashboard widget lifecycle and data retrieval path. It must not introduce a generalized alerting engine or a new telemetry/monitoring pipeline.\n\nThe widget should support:\n- A widget picker entry and persisted widget type/manifest\n- A numeric literal input or existing monitored numeric value source\n- Configurable thresholds, labels, and inclusive/exclusive comparison semantics\n- Visual rendering of green/yellow/red states plus distinct no-data and invalid-configuration states\n- Refresh behavior when the underlying value changes or the dashboard refreshes\n\n## System Changes\n- Add a new dashboard widget type/manifest so the dashboard can enumerate, instantiate, and persist the widget.\n- Extend widget configuration storage/schema to hold the selected input mode, numeric literal or source reference, threshold definitions, label text, and comparison semantics.\n- Add server-side validation for required fields, supported comparison operators, threshold ordering, overlapping or malformed ranges, and source compatibility with a numeric widget input.\n- Implement backend value resolution and status evaluation in the existing widget data path so the response includes computed status plus supporting metadata.\n- Surface no-data and invalid-configuration outcomes through the normal widget response/state model.\n- Ensure the widget is wired into the existing dashboard refresh/update lifecycle so it re-evaluates when the value changes or the dashboard refreshes.\n- Register the widget in any server-side factory/registry/seed/migration path required for fresh installs and upgrades.\n- Update frontend widget picker, editor, and renderer logic so the widget appears as a supported dashboard option and renders consistent status states.\n- Update documentation listings or module enablement references if the dashboard widget registry is discoverable through those paths.\n\n## Acceptance Criteria\n- The Traffic Light widget appears in the add-widget picker on supported installations.\n- Users can configure either a numeric value or a monitored numeric source, define thresholds and labels, save the widget, and reload it with settings preserved.\n- The widget renders green, yellow, or red according to the configured thresholds and current value.\n- The widget displays a distinct no-data state when no valid numeric value is available.\n- The widget displays a distinct invalid-configuration state when validation fails or the persisted config is incomplete/corrupt.\n- Invalid threshold configurations are rejected with a clear error message before the widget is saved.\n- The widget updates on dashboard refresh or underlying value change using the existing widget lifecycle.\n- Fresh installs and upgraded environments both expose the widget consistently.\n\n## Implementation Checklist\n### Backend\n- [ ] Add the new widget type/manifest to the dashboard widget registry/factory.\n- [ ] Extend persisted widget configuration to store input mode, source reference or numeric literal, threshold bands, labels, and comparison semantics.\n- [ ] Add server-side validation for required fields, numeric parsing, threshold ordering, overlap detection, and compatible source types.\n- [ ] Implement backend value resolution from either the configured numeric literal or the selected monitored numeric source.\n- [ ] Compute widget status and metadata for green/yellow/red, no-data, and invalid-configuration outcomes.\n- [ ] Ensure widget serialization/deserialization round-trips all new fields.\n- [ ] Add registry/seed/migration updates if required for fresh installs or upgrades.\n\n### Frontend\n- [ ] Add the widget to the add-widget picker using the existing registry data source.\n- [ ] Implement the widget edit form for input mode selection, value/source selection, threshold entries, label text, and boundary behavior.\n- [ ] Reuse existing threshold field components where possible to keep the UX consistent.\n- [ ] Render the widget body with clear visual states for green, yellow, red, no-data, and invalid-configuration.\n- [ ] Show the current numeric value and the label associated with the active threshold band.\n- [ ] Map backend status metadata to existing dashboard colors, icons, and styles.\n- [ ] Handle refresh events by updating the widget state without a full page reload.\n- [ ] Surface server-side validation errors inline in the widget configuration form.\n\n### Testing\n- [ ] Unit test backend threshold evaluation for boundary conditions, inclusive/exclusive rules, label selection, and state mapping.\n- [ ] Unit test backend configuration validation for missing fields, malformed thresholds, overlapping ranges, invalid numeric input, and incompatible source types.\n- [ ] Add backend persistence/round-trip tests to confirm saved widget settings reload exactly as configured.\n- [ ] Add API/controller tests for create, update, and read flows for the new widget type.\n- [ ] Add frontend component tests for picker visibility, edit form behavior, render states, and refresh updates.\n- [ ] Add integration tests to verify the widget can be configured, saved, reopened, and refreshed within the dashboard lifecycle.\n- [ ] Add regression coverage for upgrade or fresh-seed paths if registry, manifest, or module enablement data is required.\n- [ ] Manual verification: create the widget from the picker, save it, reload the dashboard, and confirm the saved configuration is preserved.\n- [ ] Manual verification: test each threshold boundary to confirm the color/state changes at the expected values.\n- [ ] Manual verification: verify the no-data state by pointing the widget at a source with no current numeric value or by temporarily removing the source.\n- [ ] Manual verification: verify invalid-configuration handling by saving overlapping or incomplete thresholds and confirming the rejection message.\n- [ ] Manual verification: trigger a dashboard refresh or underlying value update and confirm the widget re-renders without a full page reload.\n- [ ] Manual verification: check both a fresh-seeded environment and an upgraded environment if registry or seed data is involved.\n\n## Verification\n- Confirm the widget appears in the add-widget picker.\n- Save a configuration using both direct numeric input and a monitored numeric source, then reload and verify persistence.\n- Validate each threshold boundary to confirm the rendered status changes at the expected values.\n- Verify the no-data state by using a source with no current numeric value.\n- Verify the invalid-configuration state by submitting overlapping, incomplete, or malformed thresholds.\n- Refresh the dashboard or update the underlying value and confirm the widget updates without a full page reload.\n- Verify fresh-install and upgraded-environment behavior if registry, manifest, or seed data changes are required.\n\n## Documentation Impact\n- Update dashboard widget listings or module documentation so the new widget is documented as a supported option.\n- Update any architecture or dashboard widget docs that describe widget registration, refresh flow, or supported widget types.\n- Document the configuration semantics for threshold ordering, boundary inclusion, and no-data behavior.\n- Note any required seed, migration, or upgrade steps if the widget is introduced through registry data.\n- Add the provided PlantUML diagram to the relevant design or implementation docs if diagrams are maintained for dashboard widget flows.\n\n## Suggested Diagram\n```puml\n@startuml\nleft to right direction\nskinparam componentStyle rectangle\nskinparam shadowing false\n\nactor \"Dashboard user\" as User\ncomponent \"Widget edit form\\n(frontend)\" as FEEdit\ncomponent \"Widget registry / manifest\" as Registry\ncomponent \"Widget config persistence\" as Persist\ncomponent \"Dashboard widget backend\" as BE\ncomponent \"Value source\\n(existing monitored value\\nor numeric literal)\" as Source\ncomponent \"Threshold evaluation\" as Eval\ncomponent \"Widget renderer\\n(frontend)\" as FERender\n\nUser --> FEEdit : configure input mode\\nsource / numeric value\\nthresholds / labels\nFEEdit --> Registry : load widget type\nFEEdit --> Persist : save config\nPersist --> BE : round-trip persisted config\nBE --> Source : resolve latest numeric value\nBE --> Eval : compare value vs thresholds\nEval --> BE : status + metadata\\n(green/yellow/red\\nno-data/invalid-config)\nBE --> FERender : widget response/state\nFERender --> User : render status color\\nvalue + label\\ninline validation/errors\n\nnote right of Eval\nExplicit outcomes:\n- green / yellow / red\n- no-data\n- invalid-configuration\nend note\n\n@enduml\n```",
  "codex_prompt": "## Goal\nAdd a new dashboard widget that evaluates a configured numeric value or monitored numeric metric against thresholds and displays a traffic-light status indicator with explicit green/yellow/red, no-data, and invalid-configuration states.\n\n## Scope\nThis ticket covers a single dashboard widget type end-to-end: widget registration, persisted configuration, server-side validation, value resolution, status evaluation, refresh/update behavior, and frontend rendering. The widget must fit into the existing dashboard widget lifecycle and data retrieval path. It must not introduce a generalized alerting engine or a new telemetry/monitoring pipeline.\n\nThe widget should support:\n- A widget picker entry and persisted widget type/manifest\n- A numeric literal input or existing monitored numeric value source\n- Configurable thresholds, labels, and inclusive/exclusive comparison semantics\n- Visual rendering of green/yellow/red states plus distinct no-data and invalid-configuration states\n- Refresh behavior when the underlying value changes or the dashboard refreshes\n\n## System Changes\n- Add a new dashboard widget type/manifest so the dashboard can enumerate, instantiate, and persist the widget.\n- Extend widget configuration storage/schema to hold the selected input mode, numeric literal or source reference, threshold definitions, label text, and comparison semantics.\n- Add server-side validation for required fields, supported comparison operators, threshold ordering, overlapping or malformed ranges, and source compatibility with a numeric widget input.\n- Implement backend value resolution and status evaluation in the existing widget data path so the response includes computed status plus supporting metadata.\n- Surface no-data and invalid-configuration outcomes through the normal widget response/state model.\n- Ensure the widget is wired into the existing dashboard refresh/update lifecycle so it re-evaluates when the value changes or the dashboard refreshes.\n- Register the widget in any server-side factory/registry/seed/migration path required for fresh installs and upgrades.\n- Update frontend widget picker, editor, and renderer logic so the widget appears as a supported dashboard option and renders consistent status states.\n- Update documentation listings or module enablement references if the dashboard widget registry is discoverable through those paths.\n\n## Acceptance Criteria\n- The Traffic Light widget appears in the add-widget picker on supported installations.\n- Users can configure either a numeric value or a monitored numeric source, define thresholds and labels, save the widget, and reload it with settings preserved.\n- The widget renders green, yellow, or red according to the configured thresholds and current value.\n- The widget displays a distinct no-data state when no valid numeric value is available.\n- The widget displays a distinct invalid-configuration state when validation fails or the persisted config is incomplete/corrupt.\n- Invalid threshold configurations are rejected with a clear error message before the widget is saved.\n- The widget updates on dashboard refresh or underlying value change using the existing widget lifecycle.\n- Fresh installs and upgraded environments both expose the widget consistently.\n\n## Implementation Checklist\n### Backend\n- [ ] Add the new widget type/manifest to the dashboard widget registry/factory.\n- [ ] Extend persisted widget configuration to store input mode, source reference or numeric literal, threshold bands, labels, and comparison semantics.\n- [ ] Add server-side validation for required fields, numeric parsing, threshold ordering, overlap detection, and compatible source types.\n- [ ] Implement backend value resolution from either the configured numeric literal or the selected monitored numeric source.\n- [ ] Compute widget status and metadata for green/yellow/red, no-data, and invalid-configuration outcomes.\n- [ ] Ensure widget serialization/deserialization round-trips all new fields.\n- [ ] Add registry/seed/migration updates if required for fresh installs or upgrades.\n\n### Frontend\n- [ ] Add the widget to the add-widget picker using the existing registry data source.\n- [ ] Implement the widget edit form for input mode selection, value/source selection, threshold entries, label text, and boundary behavior.\n- [ ] Reuse existing threshold field components where possible to keep the UX consistent.\n- [ ] Render the widget body with clear visual states for green, yellow, red, no-data, and invalid-configuration.\n- [ ] Show the current numeric value and the label associated with the active threshold band.\n- [ ] Map backend status metadata to existing dashboard colors, icons, and styles.\n- [ ] Handle refresh events by updating the widget state without a full page reload.\n- [ ] Surface server-side validation errors inline in the widget configuration form.\n\n### Testing\n- [ ] Unit test backend threshold evaluation for boundary conditions, inclusive/exclusive rules, label selection, and state mapping.\n- [ ] Unit test backend configuration validation for missing fields, malformed thresholds, overlapping ranges, invalid numeric input, and incompatible source types.\n- [ ] Add backend persistence/round-trip tests to confirm saved widget settings reload exactly as configured.\n- [ ] Add API/controller tests for create, update, and read flows for the new widget type.\n- [ ] Add frontend component tests for picker visibility, edit form behavior, render states, and refresh updates.\n- [ ] Add integration tests to verify the widget can be configured, saved, reopened, and refreshed within the dashboard lifecycle.\n- [ ] Add regression coverage for upgrade or fresh-seed paths if registry, manifest, or module enablement data is required.\n- [ ] Manual verification: create the widget from the picker, save it, reload the dashboard, and confirm the saved configuration is preserved.\n- [ ] Manual verification: test each threshold boundary to confirm the color/state changes at the expected values.\n- [ ] Manual verification: verify the no-data state by pointing the widget at a source with no current numeric value or by temporarily removing the source.\n- [ ] Manual verification: verify invalid-configuration handling by saving overlapping or incomplete thresholds and confirming the rejection message.\n- [ ] Manual verification: trigger a dashboard refresh or underlying value update and confirm the widget re-renders without a full page reload.\n- [ ] Manual verification: check both a fresh-seeded environment and an upgraded environment if registry or seed data is involved.\n\n## Verification\n- Confirm the widget appears in the add-widget picker.\n- Save a configuration using both direct numeric input and a monitored numeric source, then reload and verify persistence.\n- Validate each threshold boundary to confirm the rendered status changes at the expected values.\n- Verify the no-data state by using a source with no current numeric value.\n- Verify the invalid-configuration state by submitting overlapping, incomplete, or malformed thresholds.\n- Refresh the dashboard or update the underlying value and confirm the widget updates without a full page reload.\n- Verify fresh-install and upgraded-environment behavior if registry, manifest, or seed data changes are required.\n\n## Documentation Impact\n- Update dashboard widget listings or module documentation so the new widget is documented as a supported option.\n- Update any architecture or dashboard widget docs that describe widget registration, refresh flow, or supported widget types.\n- Document the configuration semantics for threshold ordering, boundary inclusion, and no-data behavior.\n- Note any required seed, migration, or upgrade steps if the widget is introduced through registry data.\n- Add the provided PlantUML diagram to the relevant design or implementation docs if diagrams are maintained for dashboard widget flows.\n\n## Suggested Diagram\n```puml\n@startuml\nleft to right direction\nskinparam componentStyle rectangle\nskinparam shadowing false\n\nactor \"Dashboard user\" as User\ncomponent \"Widget edit form\\n(frontend)\" as FEEdit\ncomponent \"Widget registry / manifest\" as Registry\ncomponent \"Widget config persistence\" as Persist\ncomponent \"Dashboard widget backend\" as BE\ncomponent \"Value source\\n(existing monitored value\\nor numeric literal)\" as Source\ncomponent \"Threshold evaluation\" as Eval\ncomponent \"Widget renderer\\n(frontend)\" as FERender\n\nUser --> FEEdit : configure input mode\\nsource / numeric value\\nthresholds / labels\nFEEdit --> Registry : load widget type\nFEEdit --> Persist : save config\nPersist --> BE : round-trip persisted config\nBE --> Source : resolve latest numeric value\nBE --> Eval : compare value vs thresholds\nEval --> BE : status + metadata\\n(green/yellow/red\\nno-data/invalid-config)\nBE --> FERender : widget response/state\nFERender --> User : render status color\\nvalue + label\\ninline validation/errors\n\nnote right of Eval\nExplicit outcomes:\n- green / yellow / red\n- no-data\n- invalid-configuration\nend note\n\n@enduml\n```",
  "rationale": "This version tightens the scope to a single dashboard widget while making the backend, frontend, registry, and persistence boundaries explicit. It also separates automated testing from manual verification, adds a clear implementation checklist, and preserves the diagram as a concrete guide for the data flow and state model."
}
```

### Outputs
```json
{
  "data": {
    "title": "Add traffic-light status dashboard widget",
    "description": "## Goal\nAdd a new dashboard widget that evaluates a configured numeric value or monitored numeric metric against thresholds and displays a traffic-light status indicator with explicit green/yellow/red, no-data, and invalid-configuration states.\n\n## Scope\nThis ticket covers a single dashboard widget type end-to-end: widget registration, persisted configuration, server-side validation, value resolution, status evaluation, refresh/update behavior, and frontend rendering. The widget must fit into the existing dashboard widget lifecycle and data retrieval path. It must not introduce a generalized alerting engine or a new telemetry/monitoring pipeline.\n\nThe widget should support:\n- A widget picker entry and persisted widget type/manifest\n- A numeric literal input or existing monitored numeric value source\n- Configurable thresholds, labels, and inclusive/exclusive comparison semantics\n- Visual rendering of green/yellow/red states plus distinct no-data and invalid-configuration states\n- Refresh behavior when the underlying value changes or the dashboard refreshes\n\n## System Changes\n- Add a new dashboard widget type/manifest so the dashboard can enumerate, instantiate, and persist the widget.\n- Extend widget configuration storage/schema to hold the selected input mode, numeric literal or source reference, threshold definitions, label text, and comparison semantics.\n- Add server-side validation for required fields, supported comparison operators, threshold ordering, overlapping or malformed ranges, and source compatibility with a numeric widget input.\n- Implement backend value resolution and status evaluation in the existing widget data path so the response includes computed status plus supporting metadata.\n- Surface no-data and invalid-configuration outcomes through the normal widget response/state model.\n- Ensure the widget is wired into the existing dashboard refresh/update lifecycle so it re-evaluates when the value changes or the dashboard refreshes.\n- Register the widget in any server-side factory/registry/seed/migration path required for fresh installs and upgrades.\n- Update frontend widget picker, editor, and renderer logic so the widget appears as a supported dashboard option and renders consistent status states.\n- Update documentation listings or module enablement references if the dashboard widget registry is discoverable through those paths.\n\n## Acceptance Criteria\n- The Traffic Light widget appears in the add-widget picker on supported installations.\n- Users can configure either a numeric value or a monitored numeric source, define thresholds and labels, save the widget, and reload it with settings preserved.\n- The widget renders green, yellow, or red according to the configured thresholds and current value.\n- The widget displays a distinct no-data state when no valid numeric value is available.\n- The widget displays a distinct invalid-configuration state when validation fails or the persisted config is incomplete/corrupt.\n- Invalid threshold configurations are rejected with a clear error message before the widget is saved.\n- The widget updates on dashboard refresh or underlying value change using the existing widget lifecycle.\n- Fresh installs and upgraded environments both expose the widget consistently.\n\n## Implementation Checklist\n### Backend\n- [ ] Add the new widget type/manifest to the dashboard widget registry/factory.\n- [ ] Extend persisted widget configuration to store input mode, source reference or numeric literal, threshold bands, labels, and comparison semantics.\n- [ ] Add server-side validation for required fields, numeric parsing, threshold ordering, overlap detection, and compatible source types.\n- [ ] Implement backend value resolution from either the configured numeric literal or the selected monitored numeric source.\n- [ ] Compute widget status and metadata for green/yellow/red, no-data, and invalid-configuration outcomes.\n- [ ] Ensure widget serialization/deserialization round-trips all new fields.\n- [ ] Add registry/seed/migration updates if required for fresh installs or upgrades.\n\n### Frontend\n- [ ] Add the widget to the add-widget picker using the existing registry data source.\n- [ ] Implement the widget edit form for input mode selection, value/source selection, threshold entries, label text, and boundary behavior.\n- [ ] Reuse existing threshold field components where possible to keep the UX consistent.\n- [ ] Render the widget body with clear visual states for green, yellow, red, no-data, and invalid-configuration.\n- [ ] Show the current numeric value and the label associated with the active threshold band.\n- [ ] Map backend status metadata to existing dashboard colors, icons, and styles.\n- [ ] Handle refresh events by updating the widget state without a full page reload.\n- [ ] Surface server-side validation errors inline in the widget configuration form.\n\n### Testing\n- [ ] Unit test backend threshold evaluation for boundary conditions, inclusive/exclusive rules, label selection, and state mapping.\n- [ ] Unit test backend configuration validation for missing fields, malformed thresholds, overlapping ranges, invalid numeric input, and incompatible source types.\n- [ ] Add backend persistence/round-trip tests to confirm saved widget settings reload exactly as configured.\n- [ ] Add API/controller tests for create, update, and read flows for the new widget type.\n- [ ] Add frontend component tests for picker visibility, edit form behavior, render states, and refresh updates.\n- [ ] Add integration tests to verify the widget can be configured, saved, reopened, and refreshed within the dashboard lifecycle.\n- [ ] Add regression coverage for upgrade or fresh-seed paths if registry, manifest, or module enablement data is required.\n- [ ] Manual verification: create the widget from the picker, save it, reload the dashboard, and confirm the saved configuration is preserved.\n- [ ] Manual verification: test each threshold boundary to confirm the color/state changes at the expected values.\n- [ ] Manual verification: verify the no-data state by pointing the widget at a source with no current numeric value or by temporarily removing the source.\n- [ ] Manual verification: verify invalid-configuration handling by saving overlapping or incomplete thresholds and confirming the rejection message.\n- [ ] Manual verification: trigger a dashboard refresh or underlying value update and confirm the widget re-renders without a full page reload.\n- [ ] Manual verification: check both a fresh-seeded environment and an upgraded environment if registry or seed data is involved.\n\n## Verification\n- Confirm the widget appears in the add-widget picker.\n- Save a configuration using both direct numeric input and a monitored numeric source, then reload and verify persistence.\n- Validate each threshold boundary to confirm the rendered status changes at the expected values.\n- Verify the no-data state by using a source with no current numeric value.\n- Verify the invalid-configuration state by submitting overlapping, incomplete, or malformed thresholds.\n- Refresh the dashboard or update the underlying value and confirm the widget updates without a full page reload.\n- Verify fresh-install and upgraded-environment behavior if registry, manifest, or seed data changes are required.\n\n## Documentation Impact\n- Update dashboard widget listings or module documentation so the new widget is documented as a supported option.\n- Update any architecture or dashboard widget docs that describe widget registration, refresh flow, or supported widget types.\n- Document the configuration semantics for threshold ordering, boundary inclusion, and no-data behavior.\n- Note any required seed, migration, or upgrade steps if the widget is introduced through registry data.\n- Add the provided PlantUML diagram to the relevant design or implementation docs if diagrams are maintained for dashboard widget flows.\n\n## Suggested Diagram\n```puml\n@startuml\nleft to right direction\nskinparam componentStyle rectangle\nskinparam shadowing false\n\nactor \"Dashboard user\" as User\ncomponent \"Widget edit form\\n(frontend)\" as FEEdit\ncomponent \"Widget registry / manifest\" as Registry\ncomponent \"Widget config persistence\" as Persist\ncomponent \"Dashboard widget backend\" as BE\ncomponent \"Value source\\n(existing monitored value\\nor numeric literal)\" as Source\ncomponent \"Threshold evaluation\" as Eval\ncomponent \"Widget renderer\\n(frontend)\" as FERender\n\nUser --> FEEdit : configure input mode\\nsource / numeric value\\nthresholds / labels\nFEEdit --> Registry : load widget type\nFEEdit --> Persist : save config\nPersist --> BE : round-trip persisted config\nBE --> Source : resolve latest numeric value\nBE --> Eval : compare value vs thresholds\nEval --> BE : status + metadata\\n(green/yellow/red\\nno-data/invalid-config)\nBE --> FERender : widget response/state\nFERender --> User : render status color\\nvalue + label\\ninline validation/errors\n\nnote right of Eval\nExplicit outcomes:\n- green / yellow / red\n- no-data\n- invalid-configuration\nend note\n\n@enduml\n```",
    "codex_prompt": "## Goal\nAdd a new dashboard widget that evaluates a configured numeric value or monitored numeric metric against thresholds and displays a traffic-light status indicator with explicit green/yellow/red, no-data, and invalid-configuration states.\n\n## Scope\nThis ticket covers a single dashboard widget type end-to-end: widget registration, persisted configuration, server-side validation, value resolution, status evaluation, refresh/update behavior, and frontend rendering. The widget must fit into the existing dashboard widget lifecycle and data retrieval path. It must not introduce a generalized alerting engine or a new telemetry/monitoring pipeline.\n\nThe widget should support:\n- A widget picker entry and persisted widget type/manifest\n- A numeric literal input or existing monitored numeric value source\n- Configurable thresholds, labels, and inclusive/exclusive comparison semantics\n- Visual rendering of green/yellow/red states plus distinct no-data and invalid-configuration states\n- Refresh behavior when the underlying value changes or the dashboard refreshes\n\n## System Changes\n- Add a new dashboard widget type/manifest so the dashboard can enumerate, instantiate, and persist the widget.\n- Extend widget configuration storage/schema to hold the selected input mode, numeric literal or source reference, threshold definitions, label text, and comparison semantics.\n- Add server-side validation for required fields, supported comparison operators, threshold ordering, overlapping or malformed ranges, and source compatibility with a numeric widget input.\n- Implement backend value resolution and status evaluation in the existing widget data path so the response includes computed status plus supporting metadata.\n- Surface no-data and invalid-configuration outcomes through the normal widget response/state model.\n- Ensure the widget is wired into the existing dashboard refresh/update lifecycle so it re-evaluates when the value changes or the dashboard refreshes.\n- Register the widget in any server-side factory/registry/seed/migration path required for fresh installs and upgrades.\n- Update frontend widget picker, editor, and renderer logic so the widget appears as a supported dashboard option and renders consistent status states.\n- Update documentation listings or module enablement references if the dashboard widget registry is discoverable through those paths.\n\n## Acceptance Criteria\n- The Traffic Light widget appears in the add-widget picker on supported installations.\n- Users can configure either a numeric value or a monitored numeric source, define thresholds and labels, save the widget, and reload it with settings preserved.\n- The widget renders green, yellow, or red according to the configured thresholds and current value.\n- The widget displays a distinct no-data state when no valid numeric value is available.\n- The widget displays a distinct invalid-configuration state when validation fails or the persisted config is incomplete/corrupt.\n- Invalid threshold configurations are rejected with a clear error message before the widget is saved.\n- The widget updates on dashboard refresh or underlying value change using the existing widget lifecycle.\n- Fresh installs and upgraded environments both expose the widget consistently.\n\n## Implementation Checklist\n### Backend\n- [ ] Add the new widget type/manifest to the dashboard widget registry/factory.\n- [ ] Extend persisted widget configuration to store input mode, source reference or numeric literal, threshold bands, labels, and comparison semantics.\n- [ ] Add server-side validation for required fields, numeric parsing, threshold ordering, overlap detection, and compatible source types.\n- [ ] Implement backend value resolution from either the configured numeric literal or the selected monitored numeric source.\n- [ ] Compute widget status and metadata for green/yellow/red, no-data, and invalid-configuration outcomes.\n- [ ] Ensure widget serialization/deserialization round-trips all new fields.\n- [ ] Add registry/seed/migration updates if required for fresh installs or upgrades.\n\n### Frontend\n- [ ] Add the widget to the add-widget picker using the existing registry data source.\n- [ ] Implement the widget edit form for input mode selection, value/source selection, threshold entries, label text, and boundary behavior.\n- [ ] Reuse existing threshold field components where possible to keep the UX consistent.\n- [ ] Render the widget body with clear visual states for green, yellow, red, no-data, and invalid-configuration.\n- [ ] Show the current numeric value and the label associated with the active threshold band.\n- [ ] Map backend status metadata to existing dashboard colors, icons, and styles.\n- [ ] Handle refresh events by updating the widget state without a full page reload.\n- [ ] Surface server-side validation errors inline in the widget configuration form.\n\n### Testing\n- [ ] Unit test backend threshold evaluation for boundary conditions, inclusive/exclusive rules, label selection, and state mapping.\n- [ ] Unit test backend configuration validation for missing fields, malformed thresholds, overlapping ranges, invalid numeric input, and incompatible source types.\n- [ ] Add backend persistence/round-trip tests to confirm saved widget settings reload exactly as configured.\n- [ ] Add API/controller tests for create, update, and read flows for the new widget type.\n- [ ] Add frontend component tests for picker visibility, edit form behavior, render states, and refresh updates.\n- [ ] Add integration tests to verify the widget can be configured, saved, reopened, and refreshed within the dashboard lifecycle.\n- [ ] Add regression coverage for upgrade or fresh-seed paths if registry, manifest, or module enablement data is required.\n- [ ] Manual verification: create the widget from the picker, save it, reload the dashboard, and confirm the saved configuration is preserved.\n- [ ] Manual verification: test each threshold boundary to confirm the color/state changes at the expected values.\n- [ ] Manual verification: verify the no-data state by pointing the widget at a source with no current numeric value or by temporarily removing the source.\n- [ ] Manual verification: verify invalid-configuration handling by saving overlapping or incomplete thresholds and confirming the rejection message.\n- [ ] Manual verification: trigger a dashboard refresh or underlying value update and confirm the widget re-renders without a full page reload.\n- [ ] Manual verification: check both a fresh-seeded environment and an upgraded environment if registry or seed data is involved.\n\n## Verification\n- Confirm the widget appears in the add-widget picker.\n- Save a configuration using both direct numeric input and a monitored numeric source, then reload and verify persistence.\n- Validate each threshold boundary to confirm the rendered status changes at the expected values.\n- Verify the no-data state by using a source with no current numeric value.\n- Verify the invalid-configuration state by submitting overlapping, incomplete, or malformed thresholds.\n- Refresh the dashboard or update the underlying value and confirm the widget updates without a full page reload.\n- Verify fresh-install and upgraded-environment behavior if registry, manifest, or seed data changes are required.\n\n## Documentation Impact\n- Update dashboard widget listings or module documentation so the new widget is documented as a supported option.\n- Update any architecture or dashboard widget docs that describe widget registration, refresh flow, or supported widget types.\n- Document the configuration semantics for threshold ordering, boundary inclusion, and no-data behavior.\n- Note any required seed, migration, or upgrade steps if the widget is introduced through registry data.\n- Add the provided PlantUML diagram to the relevant design or implementation docs if diagrams are maintained for dashboard widget flows.\n\n## Suggested Diagram\n```puml\n@startuml\nleft to right direction\nskinparam componentStyle rectangle\nskinparam shadowing false\n\nactor \"Dashboard user\" as User\ncomponent \"Widget edit form\\n(frontend)\" as FEEdit\ncomponent \"Widget registry / manifest\" as Registry\ncomponent \"Widget config persistence\" as Persist\ncomponent \"Dashboard widget backend\" as BE\ncomponent \"Value source\\n(existing monitored value\\nor numeric literal)\" as Source\ncomponent \"Threshold evaluation\" as Eval\ncomponent \"Widget renderer\\n(frontend)\" as FERender\n\nUser --> FEEdit : configure input mode\\nsource / numeric value\\nthresholds / labels\nFEEdit --> Registry : load widget type\nFEEdit --> Persist : save config\nPersist --> BE : round-trip persisted config\nBE --> Source : resolve latest numeric value\nBE --> Eval : compare value vs thresholds\nEval --> BE : status + metadata\\n(green/yellow/red\\nno-data/invalid-config)\nBE --> FERender : widget response/state\nFERender --> User : render status color\\nvalue + label\\ninline validation/errors\n\nnote right of Eval\nExplicit outcomes:\n- green / yellow / red\n- no-data\n- invalid-configuration\nend note\n\n@enduml\n```",
    "rationale": "This version tightens the scope to a single dashboard widget while making the backend, frontend, registry, and persistence boundaries explicit. It also separates automated testing from manual verification, adds a clear implementation checklist, and preserves the diagram as a concrete guide for the data flow and state model."
  }
}
```

